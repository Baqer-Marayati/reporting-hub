# Exports

Generated handoff packages go in `Server Packages/`.

The routine export file is:
- `Server Packages/Financial Report - ready.zip`

That zip is generated by:
- `./scripts/package-report.sh`

The zip files themselves are ignored by Git so the repo stays clean.
