# Core

Use this folder for shared domain baseline assets:

- base PBIP project(s)
- shared semantic model components
- shared visual/page patterns
- shared theme files in `themes/`

Do not put company-specific overrides here.
