# Companies

Use this folder for company-specific Finance reporting configuration and overrides.

Suggested structure:

```text
Companies/
  <CompanyCode>/
    config/
    overlays/
    Exports/Server Packages/
    Records/screenshots/
```

Prefer config-first. Use overlays only when configuration cannot satisfy the requirement.
