# Core

This folder is reserved for Finance domain baseline assets shared across companies.

Current transitional state:
- Active editable PBIP is still `../Financial Report/Financial Report.pbip`.
- New shared assets should be placed in `Core/` first when they are company-agnostic.

When migration is complete, `Core/` should hold the shared baseline PBIP and reusable model/report components.
