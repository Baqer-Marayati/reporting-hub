# Overlays

Use this folder for optional company-specific overrides when config is not enough.

Examples:
- measure override fragments
- page-level visual overrides

Prefer config-first. Keep overlays small and explicit.
